Deploy `TimelockController` with `minDelay`, `proposers`, `executors`.
Grant `PROPOSER_ROLE` to the Governor and (optionally) others.
Grant `EXECUTOR_ROLE` to `address(0)` to allow anyone to execute.
